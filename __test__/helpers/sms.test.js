const Sms = require('../../helpers/sms.js');
describe('sms', () => {
    describe('send()', () => {
        test('successfully send', async () => {
            // const sms = new Sms();
            // const message = await sms.send('+819999999999', 'Hello Twilio');
            // expect(message.status).toEqual('queued');
        });
    });
});
